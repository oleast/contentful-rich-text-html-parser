import { BLOCKS, INLINES, MARKS } from "@contentful/rich-text-types";

export const BLOCK_TYPES = Object.values(BLOCKS);
export const INLINE_TYPES = Object.values(INLINES);
export const MARK_TYPES = Object.values(MARKS);
